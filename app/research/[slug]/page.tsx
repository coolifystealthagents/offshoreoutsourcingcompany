import {notFound} from 'next/navigation';import type {Metadata} from 'next';import {Header,Footer,CTA} from '../../components';import {researchPosts} from '../../data';export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);return p?{title:p.title,description:p.excerpt,alternates:{canonical:`/research/${p.slug}`},openGraph:{title:p.title,description:p.excerpt,type:'article'}}:{}}export default async function ResearchPost({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);if(!p)notFound();return <><Header/><main className="section"><article className="container guide-article"><p className="eyebrow">Philippines staffing research</p><h1>{p.title}</h1><p className="lead">{p.excerpt}</p><div className="card">{p.body.map(x=><p key={x}>{x}</p>)}</div></article><CTA/></main><Footer/></>}
